# Database path (SQLite) - relative to working directory
DATABASE_URL=./data/keys.db

# Package storage directory
PACKAGES_DIR=./data/packages

# Server binding
HOST=0.0.0.0
PORT=3000

# Admin portal credentials
ADMIN_USER=admin
ADMIN_PASS_HASH=  #  P=$(openssl rand -base64 32) && echo "Pass: $P" && echo "Hash: $(echo -n "$P" | sha256sum | awk '{print $1}')" && unset P

JWT_SECRET=  # openssl rand -base64 64
ACCESS_TOKEN_TTL=30m
REFRESH_TOKEN_TTL=1d

# CORS allowed origins (comma-separated)
CORS_ORIGINS=http://localhost:3000
